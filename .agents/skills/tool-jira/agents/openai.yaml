interface:
  display_name: "Tool Jira"
  short_description: "Repo-specific patterns and workflows for tool-jira"
  default_prompt: "Use the tool-jira repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true