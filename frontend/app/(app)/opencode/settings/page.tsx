import { OpenCodeSettings } from '@/components/opencode/opencode-settings';

export default function SettingsPage() {
  return <OpenCodeSettings />;
}

export const metadata = {
  title: 'OpenCode Settings',
};
